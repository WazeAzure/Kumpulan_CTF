DROP TABLE IF EXISTS "pwncollege-web10";

CREATE TABLE "pwncollege-web10" (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    created TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    user TEXT NOT NULL,
    password TEXT NOT NULL,
	flag TEXT NOT NULL,
	leak BOOLEAN NOT NULL
);
